export const baseUrl = 'https://localhost:8000'

export const endpoints = {
    record: {
        create: baseUrl + '/record/create'
    },
    me: baseUrl + '/me',
    mal: baseUrl + '/mal',
    token: baseUrl + '/token'
}